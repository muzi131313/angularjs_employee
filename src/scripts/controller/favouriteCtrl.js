(function () {
    'use strict';

    angular.module('app').controller('favouriteCtrl', ['$scope', '$http', '$state', 'dict', function ($scope, $http, $state, dict) {
        $scope.favouriteList = [{

        }]
    }]);
})();
